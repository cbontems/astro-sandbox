---
type Props = {
  category: string;
};
const { category } = Astro.props;
---
<a
  href={`/category/${category}`}
  class="text-teal-900 rounded-3xl uppercase text-xl py-1 px-4 border-2 border-teal-900 whitespace-nowrap"
  >{category}</a
>
